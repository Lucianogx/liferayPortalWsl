/**
 * SPDX-FileCopyrightText: © 2020 Liferay, Inc. <https://liferay.com>
 * SPDX-License-Identifier: LGPL-3.0-or-later
 */
import * as estraverse from 'estraverse';
import estree from 'estree';
import { RawSourceMap } from 'source-map';
import FilePath from '../../file/FilePath';
export interface SourceCode {
    ast?: estree.Node;
    code: string;
    map?: RawSourceMap;
}
export interface SourceTransform {
    (source: SourceCode): Promise<SourceCode>;
}
export declare function replace(source: SourceCode, visitor: estraverse.Visitor): Promise<SourceCode>;
export declare function transformSource(source: SourceCode, ...transforms: SourceTransform[]): Promise<SourceCode>;
/**
 * Transform a JavaScript file in place
 *
 * @param fromFile the file to transform
 * @param toFile the destination file
 * @param transforms the transform functions to apply
 */
export declare function transformSourceFile(fromFile: FilePath, toFile: FilePath, ...transforms: SourceTransform[]): Promise<void>;
