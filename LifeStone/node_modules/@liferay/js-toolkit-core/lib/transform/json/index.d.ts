/**
 * SPDX-FileCopyrightText: © 2020 Liferay, Inc. <https://liferay.com>
 * SPDX-License-Identifier: LGPL-3.0-or-later
 */
import FilePath from '../../file/FilePath';
export interface JsonTransform<T extends object> {
    (json: T): Promise<T>;
}
export declare function transformJson<T extends object>(json: T, ...transforms: JsonTransform<T>[]): Promise<T>;
/**
 * Transform a JSON file
 *
 * @param fromFile the source file to transform
 * @param toFile the destination file
 * @param transforms the transform functions to apply
 */
export declare function transformJsonFile<T extends object>(fromFile: FilePath, toFile: FilePath, ...transforms: JsonTransform<T>[]): Promise<void>;
