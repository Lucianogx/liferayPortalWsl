"use strict";
/**
 * SPDX-FileCopyrightText: © 2021 Liferay, Inc. <https://liferay.com>
 * SPDX-License-Identifier: LGPL-3.0-or-later
 */
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const inquirer_1 = __importDefault(require("inquirer"));
async function promptForConfiguration(prompts) {
    const initialValues = prompts.reduce((values, prompt) => {
        values[prompt.name] = prompt.default;
        return values;
    }, {});
    const answers = await inquirer_1.default.prompt(prompts);
    for (const key of Object.keys(initialValues)) {
        if (initialValues[key] === answers[key]) {
            delete answers[key];
        }
    }
    return answers;
}
exports.default = promptForConfiguration;
